<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHM Marketplace - Dự án của Linh</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold fs-3" href="/">SHM</a>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/post-ad"><i class="fas fa-plus-circle"></i> Đăng tin ngay</a>
                    </li>
                </ul>
                
                <div class="d-flex align-items-center gap-3">
                    {{#if user}}
                        <div class="dropdown">
                            <a class="btn btn-outline-light dropdown-toggle border-0" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user-circle"></i> {{user.full_name}}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                                <li><a class="dropdown-item" href="#"><i class="fas fa-list"></i> Tin đã đăng</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="/logout"><i class="fas fa-sign-out-alt"></i> Đăng xuất</a></li>
                            </ul>
                        </div>
                    {{else}}
                        <a href="/login" class="btn btn-outline-light px-4 rounded-pill">Đăng nhập</a>
                        <a href="/register" class="btn btn-warning px-4 rounded-pill fw-bold">Đăng ký</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </nav>

    <main class="container py-4">
        {{{body}}}
    </main>

    <footer class="bg-white py-4 mt-5 border-top">
        <div class="container text-center text-muted small">
            <p class="mb-1">© 2026 Sàn giao dịch C2C SHM</p>
          
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>